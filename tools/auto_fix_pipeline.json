{
  "name": "auto_fix_pipeline",
  "description": "一键修复流水线：诊断→分类→修复→验证→证据链，最多 3 轮迭代。支持 url 模式（访问指定 URL 并自动诊断修复）和页面模式（在当前打开的页面上操作）。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "url": { "type": "string", "description": "目标 URL（可选，不传则在当前页面操作）" },
      "maxIterations": { "type": "number", "default": 3, "description": "最大迭代轮数（1-3）" },
      "autoConfirm": { "type": "boolean", "default": true, "description": "是否自动执行修复，false 则仅诊断不修复" }
    }
  }
}
