{
  "name": "browser_debug",
  "description": "浏览器调试诊断工具（v1.9.5 起合并 browser_debug_report/browser_diagnose/debug_investigate）。通过 mode 参数区分子模式：report（默认，生成调试报告汇总页面状态、错误日志、网络错误）/ diagnose（自动诊断浏览器错误根因，返回 rootCause/confidence/suggestedFixes）/ investigate（输入问题症状，自动汇总 errors/events/network/DOM/storage/artifacts 并输出假设和证据链）。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "mode": {
        "type": "string",
        "description": "调试模式：report（默认，生成调试报告）/ diagnose（诊断错误根因）/ investigate（问题调查与假设）",
        "enum": ["report", "diagnose", "investigate"],
        "default": "report"
      },
      "includeDom": {
        "type": "boolean",
        "description": "是否包含页面文本和元素摘要，默认 true（mode=report 时使用）",
        "default": true
      },
      "includeStorage": {
        "type": "boolean",
        "description": "是否包含存储信息，默认 false（mode=report 时使用）；mode=investigate 时默认 true",
        "default": false
      },
      "selector": {
        "type": "string",
        "description": "目标元素选择器，诊断该元素相关的错误（mode=diagnose 时可选，不传则诊断整个页面）"
      },
      "errorType": {
        "type": "string",
        "description": "聚焦诊断的错误类型：all（全部）、js（JS错误）、network（网络错误）、element（元素问题）、interaction（交互失败），默认 all（mode=diagnose 时使用）",
        "enum": ["all", "js", "network", "element", "interaction"],
        "default": "all"
      },
      "includeStackTrace": {
        "type": "boolean",
        "description": "是否包含 JS 错误堆栈分析，默认 true（mode=diagnose 时使用）",
        "default": true
      },
      "symptom": {
        "type": "string",
        "description": "问题现象，例如：点击 AI 要点后 403（mode=investigate 时使用）"
      },
      "expected": {
        "type": "string",
        "description": "期望结果，例如：生成 AI 要点（mode=investigate 时使用）"
      },
      "focus": {
        "type": "string",
        "description": "关注的 URL/API 关键字，例如 /api/ai/summarize（mode=investigate 时使用）"
      },
      "urlContains": {
        "type": "string",
        "description": "focus 的别名，按 URL 过滤网络和事件（mode=investigate 时使用）"
      },
      "statusMin": {
        "type": "number",
        "description": "只关注状态码大于等于该值的网络记录（mode=investigate 时使用）"
      },
      "limit": {
        "type": "number",
        "description": "网络和事件最多返回条数，默认 20/50（mode=investigate 时使用）"
      },
      "includeArtifacts": {
        "type": "boolean",
        "description": "是否包含 artifacts 列表，默认 true（mode=investigate 时使用）",
        "default": true
      }
    }
  }
}
