{
  "name": "browser_navigate",
  "description": "控制浏览器导航操作。提供 url 时自动导航到该地址（无需 action），不提供 url 时执行 forward/back/refresh/reload 操作。\n\n中文详情：\n- 用途：导航到新页面或控制浏览器前进/后退/刷新\n- 何时使用：需要跳转到新 URL 时（传 url）；需要刷新/后退/前进时（传 action）\n- 参数优先级：url > action。提供 url 时自动 goto，忽略 action；未提供 url 时根据 action 执行\n- 输出：{ action: string, success: boolean, currentUrl: string, waitUntil: string }\n- 参数：\n  - url (string, 可选)：目标 URL，提供时自动导航到该地址\n  - action (string, 可选)：导航操作（未提供 url 时使用），可选 forward / back / refresh / reload，默认 refresh\n  - waitUntil (string, 可选)：等待条件，默认 domcontentloaded\n  - timeout (number, 可选)：超时毫秒数，默认 30000\n- 示例1：{\"url\":\"http://example.com\"} → 导航到 example.com\n- 示例2：{\"action\":\"refresh\",\"waitUntil\":\"networkidle\"} → 刷新并等待网络空闲",
  "inputSchema": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "目标 URL。提供时自动导航到该地址（优先于 action）"
      },
      "action": {
        "type": "string",
        "description": "导航操作（未提供 url 时使用）：forward（前进）、back（后退）、refresh（刷新）、reload（重新加载）",
        "enum": [
          "forward",
          "back",
          "refresh",
          "reload"
        ],
        "default": "refresh"
      },
      "waitUntil": {
        "type": "string",
        "description": "等待条件：domcontentloaded（DOM加载完成）、load（页面完全加载）、networkidle（网络空闲）",
        "enum": [
          "domcontentloaded",
          "load",
          "networkidle"
        ],
        "default": "domcontentloaded"
      },
      "timeout": {
        "type": "number",
        "description": "超时时间，单位毫秒，默认30000"
      }
    }
  }
}
