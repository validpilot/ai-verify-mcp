{
  "name": "browser_session",
  "description": "浏览器会话管理工具（v1.9.5 起合并 browser_session_create/switch/close + browser_sessions）。通过 mode 参数区分子模式：list（默认，列出所有会话）/ create（创建命名会话，独立上下文/cookie/storage）/ switch（切换活跃会话）/ close（关闭并删除会话）。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "mode": {
        "type": "string",
        "description": "会话模式：list（默认，列出所有会话）/ create（创建命名会话）/ switch（切换活跃会话）/ close（关闭并删除会话）",
        "enum": ["list", "create", "switch", "close"],
        "default": "list"
      },
      "name": {
        "type": "string",
        "description": "会话名称，如 free-user、pro-user（mode=create/switch/close 时使用）"
      },
      "sessionName": {
        "type": "string",
        "description": "会话名称别名（mode=create/switch/close 时使用）"
      },
      "url": {
        "type": "string",
        "description": "创建后打开的 URL（mode=create 时使用）"
      },
      "headless": {
        "type": "boolean",
        "description": "是否无头模式，默认 false（mode=create 时使用）",
        "default": false
      },
      "extensionPath": {
        "type": "string",
        "description": "要加载的 Chrome 扩展目录路径。提供后使用持久化上下文并强制 headless=false（mode=create 时使用）"
      },
      "loadExtensionPath": {
        "type": "string",
        "description": "extensionPath 的别名，要加载的 Chrome 扩展目录路径（mode=create 时使用）"
      },
      "timeout": {
        "type": "number",
        "description": "导航超时时间，默认 30000ms（mode=create 时使用）",
        "default": 30000
      }
    }
  }
}
