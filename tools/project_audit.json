{
  "name": "project_audit",
  "description": "扫描项目目录，检测常见的代码质量问题：硬编码密码/密钥、硬编码绝对路径、重复事件处理器、SQL语法错误、安全配置遗漏等。运行后返回带有严重级别的问题列表。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "projectPath": {
        "type": "string",
        "description": "要扫描的项目根目录（绝对路径）"
      },
      "severity": {
        "type": "string",
        "enum": ["all", "critical", "high", "medium", "low"],
        "description": "筛选最低严重级别，默认 all"
      }
    },
    "required": ["projectPath"]
  }
}
