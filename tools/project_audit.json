{
  "name": "project_audit",
  "description": "扫描项目目录，检测常见的代码质量问题：硬编码密码/密钥、硬编码绝对路径、SQL语法错误、CSS变量异常（循环引用、引用未定义变量、非:root区域引用未定义变量）等。运行后返回带有严重级别的问题列表。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "projectPath": {
        "type": "string",
        "description": "要扫描的项目根目录（绝对路径）"
      },
      "severity": {
        "type": "string",
        "enum": ["all", "critical", "high", "medium", "low"],
        "description": "筛选最低严重级别，默认 all"
      }
    },
    "required": ["projectPath"]
  }
}
