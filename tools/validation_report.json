{
  "name": "validation_report",
  "description": "验证报告工具（v1.9.5 起合并 validation_report_export）。通过 mode 参数区分子模式：view（默认，根据最近一次 validation_check/run/matrix 生成 Markdown/JSON 结构化验证报告，含六段式结构）/ export（导出 HTML 报告到文件，等价于已废弃的 validation_report_export）。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "mode": {
        "type": "string",
        "description": "报告模式：view（默认，生成 Markdown/JSON 报告）/ export（导出 HTML 报告到文件，等价于已废弃的 validation_report_export）",
        "enum": ["view", "export"],
        "default": "view"
      },
      "format": {
        "type": "string",
        "enum": [
          "markdown",
          "json"
        ],
        "default": "markdown",
        "description": "报告格式"
      },
      "strictSchema": {
        "type": "boolean",
        "default": true,
        "description": "是否严格遵循六段式输出结构"
      }
    }
  }
}
